<?php
/**
 * @file
 * register_hooks.module
 *
 * Bet you thought more was gonna be in here.
 */

xautoload()->registerModulePsr4(__FILE__, 'src');

/**
 * This function is does nothing on purpose.
 * @return not a thing
 */
if ( ! function_exists('noop') ) {
  function noop() {}
}
